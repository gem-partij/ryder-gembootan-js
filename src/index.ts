import MongoModel, { MongoDocument, MongoCreateResult } from "./models/MongoModel";

const _default = {
    MongoModel,
};

export {
    _default as default,
    MongoModel,
    MongoDocument,
    MongoCreateResult,
};